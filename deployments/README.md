# Deployments

`studionet.json` binds the exact local source SHA-256 to a finalized StudioNet deployment, a successful intelligent transaction, and the observed `LATEST_FINAL` state.

Deployment manifests contain public addresses and transaction evidence only. Wallet private keys, keystores, passwords, and populated environment files must remain outside this repository.
